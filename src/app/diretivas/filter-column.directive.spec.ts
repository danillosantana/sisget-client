import { FilterColumnDirective } from './filter-column.directive';

describe('FilterColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterColumnDirective();
    expect(directive).toBeTruthy();
  });
});
